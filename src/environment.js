const environment = {
  production: false,
  apiUrl: 'https://stark-mesa-12909.herokuapp.com/https://jobs.github.com/positions',
  // apiUrl: 'https://jobs.github.com/positions',
  // apiUrl: '/positions' // this works for localhost, it uses a proxy configured on packege.json
};

export default environment;
